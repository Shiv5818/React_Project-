import React from 'react';
import ReactDOM from 'react-dom/client';
import App from './App';

// here we are creating a virtual dom 
const root = ReactDOM.createRoot(document.getElementById('root'));

root.render(

    <App />

);
