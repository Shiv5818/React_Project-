// the index file is creted in the folders to import all the stuff 
// and every other file coould just import the required stuff from the index file to make the code more organized 

export {TodoContext,TodoProvider,TodoContext,useTodo} from "./TodoContext"